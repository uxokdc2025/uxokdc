# uxokdc.com — off-page SEO next steps (gated / human-published)

On-page + technical SEO/AEO is shipped and live (static meta, entity graph, FAQ, llms.txt, GSC + sitemap, IndexNow). Everything below is **off-page** — the real ranking lever for "David Cervantes" queries — and needs David to publish (account creation + public-knowledge-base edits are gated).

## 1. Wikidata item (highest single lever for a Google knowledge panel + LLM citation)

**Why gated:** Wikidata is a public knowledge base. A self-created item that leans only on self-published sources (own site, LinkedIn) can be challenged or deleted under notability/conflict-of-interest norms. It sticks best once ≥1 *independent* source exists (a published interview, conference bio, or press mention — see item 2). Create it yourself while signed in (or with me driving under your explicit go).

**Prepared item** — create at https://www.wikidata.org/wiki/Special:NewItem :
- **Label (en):** David Cervantes
- **Description (en):** American AI product designer and strategist  *(short + disambiguating — there are other David Cervantes entities, e.g. athletes; the description is what separates you)*
- **Aliases:** David Cervantes UX, uxokdc
- **Statements:**
  - `instance of` (P31) → human (Q5)
  - `occupation` (P106) → user experience designer (Q60869279); also product designer / designer
  - `country of citizenship` (P27) → United States (Q30)
  - `official website` (P856) → https://www.uxokdc.com
  - `LinkedIn personal profile ID` (P6634) → davidcervantes
  - `GitHub username` (P2037) → uxokdc2025
  - `sex or gender` (P21) → *you set this*
  - `ethnic group` / place of birth → optional, *you set*
- **Reference every statement** with https://www.uxokdc.com (and the independent source once you have one).

## 2. Get ONE independent citation (unlocks Wikidata + feeds AI answers)
Any of: a podcast/newsletter interview, a guest post on a design/AI publication, a conference or meetup speaker bio, a directory listing (e.g. ADPList mentor, Read.cv, Dribbble/Behance if you make them), or a quote in an article. One credible third-party page that names you as "AI product designer/strategist" does more than ten on-site tweaks. I can draft pitches/guest-post outlines in your voice on request.

## 3. Expand the entity graph (`sameAs`) as profiles come online
Currently verified + listed: LinkedIn, GitHub (uxokdc2025), Framer portfolio. When you want, add (and I'll wire into JSON-LD): Dribbble, Behance, X/Twitter, Medium/Substack, Crunchbase, ADPList, Read.cv, Speaker Deck. Give me the handles — I only list profiles that resolve 200 (never a 404).

## 4. More "own-a-term" essays
Live: /Intent-Centered-Design. Same template ready for **Reactive AI** and **Conversational Flow Mapping** (your other named methods). Each gets Article + DefinedTerm schema so answer engines cite you as the source. Say the word and I'll write + ship them.

## 5. Bing Webmaster Tools
IndexNow already pushes to Bing. If you want the dashboard too, sign in at bing.com/webmasters with a Microsoft account and "Import from Google Search Console" — 2 clicks, I can drive it.
